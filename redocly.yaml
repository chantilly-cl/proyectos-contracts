apis:
  main:
    root: openapi/proyectos_workflow.v1.yaml
    x-openapi-ts:
      output: dist/types-spa.d.ts   # <- aquí le dices a openapi-typescript dónde escribir

extends:
  - recommended

rules:
  operation-4xx-response: error
  info-license: warn
  tag-description: warn
